import 'package:flutter/material.dart';

class TestPrincipalView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Test Principal'),
      ),
      body: Center(
        child: Text('Test Principal'),
      ),
    );
  }
}